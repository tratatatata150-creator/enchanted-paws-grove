{
  "name": "enchanted-paws-grove",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "vercel-build": "cd frontend && npm install && npm run build && cd ../api && uv pip install --system -r requirements.txt"
  }
}
