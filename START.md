# 🌿 Запуск Enchanted Paws Grove локально

## Быстрый старт

### Вариант 1: Автоматический запуск (PowerShell)

```powershell
cd C:\Users\Administrator\enchanted-paws-grove
powershell -ExecutionPolicy Bypass -File start-local.ps1
```

### Вариант 2: Ручной запуск (2 окна PowerShell)

**Окно 1 - Backend:**
```powershell
cd C:\Users\Administrator\enchanted-paws-grove\backend
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```

**Окно 2 - Frontend:**
```powershell
cd C:\Users\Administrator\enchanted-paws-grove\frontend
npm install
npm run dev
```

## Проверка работы

1. **Backend API**: http://localhost:8000/api/health
   - Должен вернуть: `{"status":"ok","game":"Enchanted Paws Grove","version":"1.0.0"}`

2. **API Документация**: http://localhost:8000/api/docs
   - Swagger UI со всеми эндпоинтами

3. **Frontend**: http://localhost:5173
   - Главная страница игры

## Что делать если не работает

### Ошибка: "python не найден"
- Установите Python 3.9+: https://www.python.org/downloads/
- Убедитесь, что Python добавлен в PATH

### Ошибка: "npm не найден"
- Установите Node.js 16+: https://nodejs.org/
- Перезапустите PowerShell после установки

### Ошибка: "Cannot connect to backend"
- Убедитесь, что backend запущен на порту 8000
- Проверьте файл `frontend\.env` - должен быть `VITE_API_URL=http://localhost:8000/api`

### Ошибка: "Module not found"
- Удалите папки `node_modules` и `package-lock.json` в frontend
- Запустите заново `npm install`

### Ошибка: "Port already in use"
Backend (8000):
```powershell
netstat -ano | findstr :8000
taskkill /PID <номер_процесса> /F
```

Frontend (5173):
```powershell
netstat -ano | findstr :5173
taskkill /PID <номер_процесса> /F
```

## Конфигурация

Все настройки в файлах `.env`:

- `backend\.env` - настройки бэкенда (токен бота, база данных)
- `frontend\.env` - URL бэкенда и имя бота
- `bot\.env` - настройки Telegram бота (для отдельного запуска)

## Тестирование в Telegram

**ВАЖНО**: В режиме разработки игра работает БЕЗ Telegram!

Вы можете открыть http://localhost:5173 в обычном браузере и тестировать игру.

Для полноценного тестирования через Telegram:
1. Деплой на облачный хостинг (когда всё работает локально)
2. Получить HTTPS URL
3. Настроить бота через BotFather с командой `/setmenubutton`

## Структура проекта

```
enchanted-paws-grove/
├── backend/          # FastAPI бэкенд
│   ├── app/         # Код приложения
│   │   ├── main.py  # Главный файл
│   │   ├── routes/  # API эндпоинты
│   │   └── ...
│   └── .env         # Конфигурация бэкенда
├── frontend/        # React фронтенд
│   ├── src/         # Исходный код
│   │   ├── App.tsx  # Главный компонент
│   │   ├── store.ts # Состояние игры
│   │   └── ...
│   └── .env         # Конфигурация фронтенда
└── bot/             # Telegram бот (опционально)
```

## API Endpoints

- `POST /api/auth/telegram` - Авторизация через Telegram
- `GET /api/game/state` - Получить состояние игры
- `POST /api/game/save` - Сохранить состояние
- `POST /api/game/merge` - Слияние существ
- `POST /api/game/collect` - Сбор ресурсов
- `GET /api/shop/items` - Список товаров
- `POST /api/shop/buy` - Покупка товара
- `GET /api/quests/daily` - Ежедневные задания
- `POST /api/quests/claim` - Получить награду за квест
- И другие...

## Полезные команды

```powershell
# Проверить версии
python --version
node --version
npm --version

# Очистить кэш
cd frontend
rm -r node_modules, package-lock.json
npm install

# Пересоздать базу данных
cd backend
rm enchanted_paws.db
# База пересоздастся автоматически при запуске

# Просмотр логов
# Логи выводятся прямо в консоль
```
